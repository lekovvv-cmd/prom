# Deployment

Compose is for local and test environments. Production deploys immutable
images, separate service/database credentials, non-root runtimes, readiness
and liveness probes, and resource limits appropriate to measured workload.
Seed data is a local operation and is not a production-startup requirement.


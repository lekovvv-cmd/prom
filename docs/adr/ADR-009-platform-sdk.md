# ADR-009: Infrastructure-only platform SDK

The SDK shares auth, errors, observability, health, storage, outbox, and test
primitives. It must never contain Projects or Service Desk business models.


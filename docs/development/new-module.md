# Creating a module

```powershell
.\dev.cmd create-module documents
```

The generator creates bounded backend/frontend roots, health endpoints, a
manifest, lazy routes, a query-key factory, test placeholder, Dockerfile, and
README. It deliberately does not create business entities, database tables, or
permissions beyond the example access permission.

